{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "actionId": {
      "type": "string",
      "description": "Action id if the user has responded to the notification, or null if he hasn't yet"
    }
  }
}

{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "opId": {
      "type": "string"
    },
    "redirectUrl": {
      "type": "string",
      "format": "uri",
      "description": "URL you should redirect (http 302) the browser to. This will only be set if you specified a callback URL"
    }
  },
  "required": [
    "opId"
  ]
}
{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "userRef": {
      "type": "string",
      "description": "User ref to assign to service link"
    },
    "url": {
      "type": "string",
      "format": "uri",
      "description": "URL on service website where the user will be redirected to after accepting the link request"
    },
    "securityLevel": {
      "type": "string",
      "description": "Security level for the approval request",
      "enum": [
        "LOW",
        "STANDARD",
        "HIGH",
        "VERY_HIGH"
      ]
    }
  },
  "required": [
    "userRef"
  ]
}
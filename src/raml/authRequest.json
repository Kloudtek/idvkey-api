{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "callbackUrl": {
      "type": "string",
      "format": "uri",
      "description": "URL to redirect user to after he's been responded to the authentication request"
    }
    "securityLevel": {
      "type": "string",
      "description": "Security level for the authentication request (if not set will default to website suggested security level)",
      "enum": [
        "LOW",
        "STANDARD",
        "HIGH",
        "VERY_HIGH"
      ]
    }
  },
  "required": [
    "redirectUrl"
  ]
}